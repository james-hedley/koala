% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/period_fmt.R
\name{period_fmt}
\alias{period_fmt}
\title{Format a numeric time period into readable text}
\usage{
period_fmt(period, unit = "years", digits = 1)
}
\arguments{
\item{period}{Numeric vector of time periods to format, expressed in \code{unit}.}

\item{unit}{Character vector giving the unit \code{period} is expressed in.
Recycled to the length of \code{period}. One of \code{"seconds"}, \code{"minutes"},
\code{"hours"}, \code{"days"}, \code{"weeks"}, \code{"months"}, or \code{"years"}. Default \code{"years"}.}

\item{digits}{Integer number of decimal places to show in the formatted
value. Default \code{1}.}
}
\value{
A character vector the same length as \code{period}, giving each value
formatted as \code{"<number> <unit>"} (e.g. \code{"2.9 days"}), with the unit
singularised only when \code{digits = 0} and the rounded value is exactly 1
(e.g. \code{"1 month"}). At non-zero \code{digits}, the plural is always used
(e.g. \code{"1.0 years"}), since a decimal value reads more naturally as plural.
}
\description{
Converts a numeric period (e.g. a decimal number of years) into a
human-readable string, automatically choosing the largest time unit
(years, months, weeks, days, hours, minutes, or seconds) for which the
value is at least 1. Avoids awkward output such as "0.417 weeks" or
"70.128 hours" by picking a more natural unit. Vectorised over \code{period}
and \code{unit}.
}
\examples{
period_fmt(0.008, unit = "years")
# "2.9 days"

period_fmt(c(0.09, 27.65, 1.2), unit = "months", digits = 0)
# "3 days"  "2 years" "1 month"

period_fmt(1, unit = "years", digits = 1)
# "1.0 years"

period_fmt(1, unit = "years", digits = 0)
# "1 year"

}
